import * as ts from "typescript";
import { EmitHost, TranspiledFile } from "./transpile";
export interface OutputFile {
    name: string;
    text: string;
}
export declare function emitTranspiledFiles(program: ts.Program, transpiledFiles: TranspiledFile[], emitHost?: EmitHost): OutputFile[];
